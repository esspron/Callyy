# LiveKit Voice Agent Dependencies
# For real-time voice AI with ultra-low latency

# LiveKit Core (v1.3.x with silero and turn-detector)
livekit-agents[silero,turn-detector]~=1.3

# LiveKit Plugins (self-hosted - not using LiveKit Cloud inference)
livekit-plugins-openai~=1.0
livekit-plugins-deepgram~=1.0
livekit-plugins-elevenlabs~=1.0
livekit-plugins-google~=1.0

# Database & HTTP
supabase>=2.0.0
httpx>=0.27.0
aiohttp>=3.9.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
structlog>=24.0.0
